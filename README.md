# React + Tailwind CSS Hello World

A simple Hello World application built with React and Tailwind CSS using JavaScript.

## Getting Started

### Installation

Install the dependencies:

```bash
npm install
```

### Development

Run the development server:

```bash
npm run dev
```

The application will be available at `http://localhost:5173`

### Build

Build for production:

```bash
npm run build
```

### Preview

Preview the production build:

```bash
npm run preview
```

## Push to GitHub

Push this application to GitHub! See [GITHUB_SETUP.md](./GITHUB_SETUP.md) for detailed instructions.

**Quick push (after installing Git):**
```bash
cd frontend
.\push-to-github.ps1
```

Or manually:
```bash
git init
git add .
git commit -m "Initial commit: React + Tailwind CSS Hello World app"
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

## Deployment

Deploy to the cloud easily! See [DEPLOYMENT.md](./DEPLOYMENT.md) for detailed instructions.

**Quick deploy with Vercel:**
```bash
npm install -g vercel
vercel login
vercel
```

## Technologies

- React 18
- Tailwind CSS 3
- Vite
- JavaScript (ES6+)


